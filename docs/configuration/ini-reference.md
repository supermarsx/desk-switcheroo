---
title: Advanced INI Reference
nav_order: 2
parent: Configuration
---

# Advanced INI Reference

This page documents every key Desk Switcheroo reads from its configuration file,
`desk_switcheroo.ini`. It is generated by reading `includes/Config.au3` top to bottom:
the tables follow the exact order the app loads the sections in (`_Cfg_Load` in
`includes/Config.au3`), and every default, clamp range, and enumeration below is the value
that function passes to its typed readers (`__Cfg_ReadBool`, `__Cfg_ReadInt`, `__Cfg_ReadEnum`,
or a raw `IniRead`).

The file lives next to the executable and is created on first run — see
[Configuration](index.md) for how it is created, watched, and saved. For the command-line
interface, see [CLI Parameters](cli.md).

## How to read these tables

Each section is a `[Section]` header in the INI. Columns:

- **Key** — the INI key name (case-sensitive as written).
- **Type** — how the value is parsed: `bool` (`true`/`false`, also accepts `1`/`0`),
  `int` (whole number, clamped to the range shown), `enum` (one of a fixed set of strings),
  or `string` (free text).
- **Default** — the value used when the key is missing or unreadable. Integers out of range are
  clamped to the nearest bound; invalid enum values fall back to the default.
- **Range / Values** — the clamp range for integers or the allowed set for enums.
- **Settings UI** — where the key is editable in the **Settings** dialog: `Yes (Tab)` names the
  tab, `INI-only` means there is no control for it (edit the file directly).
- **Notes** — behavior notes, including the two restart-required keys. Unless a note says
  otherwise, changes are applied live when you click **Apply**/**OK** in Settings
  (`__CD_ApplyChanges` → `_ApplySettingsLive`).

Types, defaults, and ranges are all taken from `includes/Config.au3`. The **Settings UI** column
was verified against `includes/ConfigDialog.au3` by checking, for each key, whether the dialog's
apply path calls that key's setter — 204 of the keys below are wired to a control; the ones marked
`INI-only` are not.

## `[General]`

Widget placement, startup, and top-level behavior toggles. Editable on the **General** tab.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `language` | string | `en-US` | any installed locale code | Yes (General) | ISO locale code (e.g. `pt-PT`, `de-DE`). **Restart required** — locale strings load once at startup. |
| `start_with_windows` | bool | `false` | | Yes (General) | Adds/removes the Windows startup registry entry. |
| `wrap_navigation` | bool | `true` | | Yes (General) | Going past the last desktop wraps to the first (and vice versa). |
| `auto_create_desktop` | bool | `false` | | Yes (General) | Next on the last desktop creates a new one instead of stopping. |
| `number_padding` | int | `2` | 1–4 | Yes (General) | Zero-pads the desktop number on the widget (e.g. `01`). |
| `widget_position` | enum | `bottom-left` | see below | Yes (General) | Screen anchor for the widget. |
| `widget_offset_x` | int | `0` | -9999–9999 | Yes (General) | Fine-tune horizontal position, in pixels. |
| `widget_offset_y` | int | `0` | -9999–9999 | Yes (General) | Fine-tune vertical position, in pixels. |
| `widget_width` | int | `0` | 0–500 | Yes (General) | Override widget width in pixels; `0` = auto (theme default). |
| `widget_height` | int | `0` | 0–200 | Yes (General) | Override widget height in pixels; `0` = auto (taskbar height). |
| `widget_drag_enabled` | bool | `false` | | Yes (General) | Allow dragging the widget to reposition it (persists the offset). |
| `widget_color_bar` | bool | `false` | | Yes (General) | Show the per-desktop color accent bar on the widget. |
| `widget_color_bar_height` | int | `2` | 1–10 | Yes (General) | Height of the color bar, in pixels. |
| `widget_color_bar_anim` | enum | `grow` | `none`, `grow`, `fade` | Yes (General) | How the color bar transitions on a desktop change: `none` (instant, the classic behavior), `grow` (bar width sweeps 0 → full with an ease-out curve), `fade` (background color lerps to the target). Only animates when `animations_enabled` is on and the mode is not `none`. |
| `widget_color_bar_anim_duration` | int | `300` | 50–2000 | Yes (General) | Duration of the `grow`/`fade` color-bar animation, in milliseconds. |
| `tray_icon_mode` | bool | `false` | | Yes (General) | Run as a system-tray icon instead of a taskbar widget. |
| `quick_access_enabled` | bool | `false` | | Yes (General) | Double-click the widget number to type a desktop number (1–9). |
| `start_minimized` | bool | `false` | | Yes (General) | Start with the widget hidden. |
| `list_keyboard_nav` | bool | `false` | | Yes (General) | Up/Down arrow keys navigate the desktop list. |
| `auto_update_enabled` | bool | `false` | | Yes (General) | Periodically check GitHub for a new release. |
| `auto_update_interval` | int | `168` | 1–720 | Yes (General) | Hours between automatic update checks (168 = weekly). |
| `singleton_enabled` | bool | `true` | | Yes (General) | Only one instance runs; relaunching kills the previous one. Read directly from the INI before the config system initializes (`desktop_switcher.au3`). |
| `min_desktops` | int | `0` | 0–50 | Yes (General) | Ensure at least this many desktops exist at startup; `0` = no minimum. |
| `max_desktops` | int | `0` | 0–50 | Yes (General) | Cap the number of desktops the app will create; `0` = unlimited. |
| `taskbar_focus_trick` | bool | `false` | | Yes (General) | Focus workaround applied around desktop switches. |
| `auto_focus_after_switch` | bool | `false` | | Yes (General) | Focus a window on the target desktop after switching. |
| `disable_win_widgets` | bool | `false` | | Yes (General) | Suppress the Windows 11 widgets flyout interference. |
| `desktop_list_pinned` | bool | `false` | | INI-only | Whether the desktop list panel stays open (pinned). Managed at runtime by the list's pin toggle; persisted here. |

### Widget anchoring

`widget_position` accepts the nine anchor points below (validated in `_Cfg_Load` against the full
list `bottom-left|bottom-center|bottom-right|middle-left|middle-right|top-left|top-center|top-right`,
plus the three legacy values `left|center|right`):

```
top-left       top-center       top-right
middle-left                     middle-right
bottom-left    bottom-center    bottom-right
```

Bottom anchors track the taskbar's vertical edge; the others use absolute screen coordinates.
The legacy values `left`, `center`, and `right` map to the corresponding `bottom-*` anchor.
`widget_offset_x` / `widget_offset_y` nudge the widget from its anchor.

## `[Updates]`

Startup update check. Editable on the **Updates** tab.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `update_check_on_startup` | bool | `false` | | Yes (Updates) | Check for a new release once at launch. |
| `update_check_days` | int | `7` | 1–90 | Yes (Updates) | Minimum days between startup checks. |

## `[Display]`

Widget count badge, fonts, thumbnails, and desktop-list sizing. Editable on the **Display** tab.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `show_count` | bool | `false` | | Yes (Display) | Show the total desktop count on the widget. |
| `count_font_size` | int | `7` | 4–20 | Yes (Display) | Font size of the count badge. |
| `theme_alpha_main` | int | `235` | 50–255 | Yes (Display) | Widget opacity (255 = opaque). |
| `theme` | enum | `dark` | `dark`, `darker`, `midnight`, `midday`, `sunset` | Yes (Display) | Color scheme. **Restart required** — the scheme is applied at startup. See [Coloring & Theming](../guides/coloring.md). |
| `thumbnails_enabled` | bool | `false` | | Yes (Display) | Show desktop thumbnails in the list. |
| `thumbnail_width` | int | `160` | 80–320 | Yes (Display) | Thumbnail width, in pixels. |
| `thumbnail_height` | int | `90` | 45–180 | Yes (Display) | Thumbnail height, in pixels. |
| `list_font_name` | string | *(empty)* | any installed font | Yes (Display) | Font for desktop-list rows; empty = theme default. |
| `list_font_size` | int | `8` | 6–14 | Yes (Display) | Desktop-list font size. |
| `list_scrollable` | bool | `false` | | Yes (Display) | Enable scroll arrows when there are more desktops than fit. |
| `list_max_visible` | int | `10` | 3–30 | Yes (Display) | Maximum desktop rows shown before scrolling. |
| `list_scroll_speed` | int | `1` | 1–5 | Yes (Display) | Rows scrolled per wheel step in the list. |
| `tooltip_font_size` | int | `8` | 6–12 | Yes (Display) | Tooltip font size. |
| `thumbnail_use_screenshot` | bool | `false` | | Yes (Display) | Use a live screenshot for thumbnails instead of a placeholder. |
| `thumbnail_cache_ttl` | int | `30` | 5–300 | Yes (Display) | Seconds a cached thumbnail is reused. |
| `desktop_list_show_numbers` | bool | `true` | | Yes (Display) | Show the desktop number in each list row. |

## `[Scroll]`

Mouse-wheel behavior on the widget and list. Editable on the **General** tab (Scroll subsection).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `scroll_enabled` | bool | `false` | | Yes (General) | Mouse wheel on the widget cycles desktops. |
| `scroll_direction` | enum | `normal` | `normal`, `inverted` | Yes (General) | Direction of wheel cycling. |
| `scroll_wrap` | bool | `true` | | Yes (General) | Wheel cycling wraps around at the ends. |
| `list_scroll_enabled` | bool | `false` | | Yes (General) | Mouse wheel over the desktop list panel is active. |
| `list_scroll_action` | enum | `switch` | `switch`, `scroll` | Yes (General) | Wheel over the list either switches desktops or scrolls the list. |

## `[Hotkeys]`

Global keyboard shortcuts. Editable on the **Hotkeys** tab. Chords use AutoIt `HotKeySet`
syntax: `^` = Ctrl, `!` = Alt, `+` = Shift, `#` = Win, and named keys in braces (`{RIGHT}`,
`{TAB}`, `{INSERT}`). An empty value means the shortcut is unbound. All hotkey keys are read with
a raw `IniRead`.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `hotkeys_enabled` | bool | `true` | | Yes (Hotkeys) | Master toggle for all global hotkeys. |
| `hotkey_next` | string | `^!{RIGHT}` | chord | Yes (Hotkeys) | Switch to the next desktop. |
| `hotkey_prev` | string | `^!{LEFT}` | chord | Yes (Hotkeys) | Switch to the previous desktop. |
| `hotkey_desktop_1` … `hotkey_desktop_9` | string | *(empty)* | chord | Yes (Hotkeys) | Jump directly to desktop 1–9. Nine separate keys. |
| `hotkey_move_to_desktop_1` … `hotkey_move_to_desktop_9` | string | *(empty)* | chord | Yes (Hotkeys) | Send the active window to desktop 1–9. Nine separate keys. |
| `hotkey_toggle_list` | string | `^!{DOWN}` | chord | Yes (Hotkeys) | Show/hide the desktop list panel. |
| `hotkey_toggle_last` | string | `^!{TAB}` | chord | Yes (Hotkeys) | Switch back to the previous desktop. |
| `hotkey_move_follow_next` | string | `^!+{RIGHT}` | chord | Yes (Hotkeys) | Move active window to the next desktop and follow it. |
| `hotkey_move_follow_prev` | string | `^!+{LEFT}` | chord | Yes (Hotkeys) | Move active window to the previous desktop and follow it. |
| `hotkey_move_next` | string | `^#{RIGHT}` | chord | Yes (Hotkeys) | Move active window to the next desktop (stay put). |
| `hotkey_move_prev` | string | `^#{LEFT}` | chord | Yes (Hotkeys) | Move active window to the previous desktop (stay put). |
| `hotkey_send_new_desktop` | string | `^!n` | chord | Yes (Hotkeys) | Create a desktop and move the active window to it. |
| `hotkey_pin_window` | string | `^!p` | chord | Yes (Hotkeys) | Pin/unpin the active window across desktops. |
| `hotkey_toggle_window_list` | string | `^!w` | chord | Yes (Hotkeys) | Show/hide the window list panel. |
| `hotkey_open_settings` | string | `^!s` | chord | Yes (Hotkeys) | Open the Settings dialog. |
| `hotkey_add_desktop` | string | `^!{INSERT}` | chord | Yes (Hotkeys) | Create a new desktop. |
| `hotkey_delete_desktop` | string | *(empty)* | chord | Yes (Hotkeys) | Delete the current desktop. |
| `hotkey_rename_desktop` | string | `^!r` | chord | Yes (Hotkeys) | Rename the current desktop. |
| `hotkey_close_window` | string | *(empty)* | chord | Yes (Hotkeys) | Close the active window. |
| `hotkey_minimize_window` | string | *(empty)* | chord | Yes (Hotkeys) | Minimize the active window. |
| `hotkey_maximize_window` | string | *(empty)* | chord | Yes (Hotkeys) | Maximize the active window. |
| `hotkey_restore_window` | string | *(empty)* | chord | Yes (Hotkeys) | Restore the active window. |
| `hotkey_toggle_slideshow` | string | *(empty)* | chord | Yes (Hotkeys) | Start/stop the desktop slideshow. Reads the legacy `hotkey_toggle_carousel` value once on first load, then that legacy key is removed on save. |
| `hotkey_task_view` | string | *(empty)* | chord | Yes (Hotkeys) | Open Windows Task View. |
| `hotkey_toggle_rules` | string | *(empty)* | chord | Yes (Hotkeys) | Toggle the window rules engine. |
| `hotkey_load_next_profile` | string | *(empty)* | chord | Yes (Hotkeys) | Load the next saved profile. |
| `hotkey_load_prev_profile` | string | *(empty)* | chord | Yes (Hotkeys) | Load the previous saved profile. |
| `hotkey_toggle_osd` | string | *(empty)* | chord | Yes (Hotkeys) | Toggle the on-screen display. |
| `hotkey_toggle_widget` | string | *(empty)* | chord | Yes (Hotkeys) | Show/hide the widget. |
| `hotkey_swap_desktops` | string | *(empty)* | chord | Yes (Hotkeys) | Swap the current desktop with an adjacent one. |
| `hotkey_gather_windows` | string | *(empty)* | chord | Yes (Hotkeys) | Gather windows onto the current desktop. |
| `hotkey_toggle_session` | string | *(empty)* | chord | Yes (Hotkeys) | Toggle session restore. |

## `[Behavior]`

Confirmations, timers, and the config watcher. Editable on the **Behavior** tab, except the three
polling intervals noted below.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `confirm_delete` | bool | `true` | | Yes (Behavior) | Confirm before deleting a desktop. |
| `middle_click_delete` | bool | `false` | | Yes (Behavior) | Middle-click a list row to delete that desktop. |
| `move_window_enabled` | bool | `true` | | Yes (Behavior) | Show "Move window here" in menus. |
| `peek_bounce_delay` | int | `500` | 100–5000 | Yes (Behavior) | Milliseconds before a peek bounces back. |
| `auto_hide_timeout` | int | `3000` | 500–30000 | Yes (Behavior) | Milliseconds before the desktop list auto-hides. |
| `topmost_interval` | int | `300` | 100–5000 | Yes (Behavior) | Milliseconds between re-asserting the widget's topmost state. |
| `cm_auto_hide_delay` | int | `500` | 100–5000 | Yes (Behavior) | Milliseconds before the widget's own menu auto-hides. |
| `ctx_auto_hide_delay` | int | `750` | 100–5000 | Yes (Behavior) | Milliseconds before a context menu auto-hides. |
| `config_watcher_enabled` | bool | `false` | | Yes (Behavior) | Watch the INI for external edits and hot-reload. |
| `config_watcher_interval` | int | `60000` | 5000–300000 | Yes (Behavior) | Milliseconds between config-file modification checks. |
| `name_sync_interval` | int | `2000` | 500–60000 | INI-only | Milliseconds between syncing OS desktop names (Windows 11). |
| `dll_check_interval` | int | `30000` | 5000–300000 | INI-only | Milliseconds between `VirtualDesktopAccessor.dll` health checks. |
| `update_poll_interval` | int | `500` | 100–5000 | INI-only | Milliseconds between background update-download polls. |
| `confirm_quit` | bool | `false` | | Yes (Behavior) | Confirm before quitting. |
| `confirm_restart` | bool | `false` | | Yes (Behavior) | Confirm before restarting. |
| `debug_mode` | bool | `false` | | Yes (Behavior) | Enable debug affordances (e.g. "Trigger crash" in the menu). |
| `disable_native_osd` | bool | `false` | | Yes (Behavior) | Suppress the app's own OSD in favor of the OS one. |

## `[Animations]`

Fade timing and per-surface animation toggles. Editable on the **Animations** tab.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `animations_enabled` | bool | `true` | | Yes (Animations) | Master toggle for all fades. |
| `fade_in_duration` | int | `80` | 0–500 | Yes (Animations) | Total fade-in time, in milliseconds. |
| `fade_out_duration` | int | `80` | 0–500 | Yes (Animations) | Total fade-out time, in milliseconds. |
| `fade_step` | int | `30` | 5–255 | Yes (Animations) | Alpha increment per frame; higher = faster/coarser. |
| `toast_fade_out_duration` | int | `300` | 0–1000 | Yes (Animations) | Toast fade-out time, in milliseconds. |
| `fade_sleep_ms` | int | `8` | 1–50 | Yes (Animations) | Sleep per fade frame, in milliseconds. |
| `anim_list` | bool | `true` | | Yes (Animations) | Animate the desktop list. |
| `anim_menus` | bool | `true` | | Yes (Animations) | Animate context menus. |
| `anim_dialogs` | bool | `true` | | Yes (Animations) | Animate dialogs (Settings, About, rename, confirm). |
| `anim_toasts` | bool | `true` | | Yes (Animations) | Animate toast notifications. |
| `anim_widget` | bool | `true` | | Yes (Animations) | Animate widget show/hide. |
| `toast_position` | enum | `widget` | `top-left`, `top-right`, `bottom-left`, `bottom-right`, `widget` | Yes (Animations) | Where toasts appear; `widget` = next to the widget. |

## `[Logging]`

File logging. Editable on the **Logging** tab. See [Logging & Diagnostics](../guides/logging.md)
for the full workflow.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `logging_enabled` | bool | `false` | | Yes (Logging) | Enable writing a log file. |
| `log_folder` | string | *(empty)* | folder path | Yes (Logging) | Log destination; empty = script directory. Expands `%APPDATA%`, `%TEMP%`, and `%SCRIPTDIR%` (`includes/Logger.au3`). |
| `log_level` | enum | `info` | `error`, `warn`, `info`, `debug` | Yes (Logging) | Verbosity; `debug` is the most detailed. |
| `log_max_size_mb` | int | `5` | 1–50 | Yes (Logging) | Size at which the log rotates, in megabytes. |
| `log_rotate_count` | int | `3` | 1–10 | Yes (Logging) | Number of rotated log files to keep. |
| `log_compress_old` | bool | `false` | | Yes (Logging) | Zip rotated logs via a detached `Compress-Archive` (never blocks the UI). |
| `log_include_pid` | bool | `false` | | Yes (Logging) | Add `[PID:xxxx]` to each log line. |
| `log_date_format` | enum | `iso` | `iso`, `us`, `eu` | Yes (Logging) | Timestamp format: ISO `YYYY-MM-DD`, US `MM/DD`, EU `DD/MM`. |
| `log_flush_immediate` | bool | `true` | | Yes (Logging) | Flush after every write (safer, slightly slower). |

## `[DesktopColors]`

Per-desktop accent colors. The enable toggle and the per-desktop color pickers are on the
**Desktops** tab. See [Coloring & Theming](../guides/coloring.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `desktop_colors_enabled` | bool | `false` | | Yes (Desktops) | Master toggle for per-desktop colors. |
| `desktop_1_color` … `desktop_50_color` | string | `0` | `0xRRGGBB` | Yes (Desktops) | Accent color for desktop *N*. `0x000000` (or unset) = no color. One key per desktop, up to 50. |

```ini
[DesktopColors]
desktop_colors_enabled=true
desktop_1_color=0x4A9EFF
desktop_2_color=0x4AFF7E
```

## `[Wallpaper]`

Per-desktop wallpaper. The enable toggle and per-desktop pickers are on the **Desktops** tab.
See [Coloring & Theming](../guides/coloring.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `wallpaper_enabled` | bool | `false` | | Yes (Desktops) | Apply a per-desktop wallpaper on switch. |
| `wallpaper_change_delay` | int | `200` | 50–2000 | Yes (Desktops) | Milliseconds to wait after a switch before setting the wallpaper. |
| `desktop_1_wallpaper` … `desktop_50_wallpaper` | string | *(empty)* | image file path | Yes (Desktops) | Wallpaper image for desktop *N*. Paths are validated on load (invalid paths are dropped). One key per desktop, up to 50. |

## `[Pinning]`

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `pinning_enabled` | bool | `false` | | Yes (Behavior) | Allow pinning windows so they appear on all desktops. |

## `[WindowList]`

The window list panel (per-window send-to-desktop / pin). Editable on the **Window List** tab.
See [Desktop Management](../guides/desktop-management.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `window_list_enabled` | bool | `false` | | Yes (Window List) | Enable the window list panel. |
| `window_list_position` | enum | `top-left` | nine-anchor grid (`top-left` … `bottom-right`) | Yes (Window List) | Anchor for the panel. |
| `window_list_width` | int | `280` | 150–600 | Yes (Window List) | Panel width, in pixels. |
| `window_list_max_visible` | int | `15` | 5–50 | Yes (Window List) | Maximum rows before scrolling. |
| `window_list_search` | bool | `true` | | Yes (Window List) | Show the search box. |
| `window_list_auto_refresh` | bool | `true` | | Yes (Window List) | Refresh the list on a timer. |
| `window_list_refresh_interval` | int | `1000` | 500–10000 | Yes (Window List) | Auto-refresh interval, in milliseconds. |
| `window_list_draggable` | bool | `false` | | Yes (Window List) | Allow dragging the panel to reposition it. |
| `window_list_scope` | enum | `current` | `current`, `all` | Yes (Window List) | Show windows from the current desktop only, or all desktops. |
| `window_list_custom_x` | int | `-1` | -1–20000 | INI-only | Custom X position set by dragging; `-1` = use the anchor. |
| `window_list_custom_y` | int | `-1` | -1–20000 | INI-only | Custom Y position set by dragging; `-1` = use the anchor. |
| `window_list_pinned` | bool | `false` | | INI-only | Whether the panel stays open (pinned). Managed at runtime by the pin toggle. |

## `[ExplorerMonitor]`

Watches the shell process (`explorer.exe`) and recovers the widget after an Explorer restart.
Editable on the **Explorer** tab. See [Stability & Mitigations](../reference/stability.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `explorer_monitor_enabled` | bool | `false` | | Yes (Explorer) | Enable the Explorer-crash monitor. |
| `shell_process_name` | string | `explorer.exe` | executable name | Yes (Explorer) | Shell process to watch; validated as an exe name on load. |
| `explorer_check_interval` | int | `5000` | 2000–60000 | Yes (Explorer) | Milliseconds between shell-process checks. |
| `monitor_max_retries` | int | `0` | 0–100 | Yes (Explorer) | Recovery attempts; `0` = unlimited. |
| `monitor_retry_delay` | int | `5000` | 1000–60000 | Yes (Explorer) | Milliseconds between recovery attempts. |
| `monitor_exp_backoff` | bool | `true` | | Yes (Explorer) | Exponentially increase the retry delay. |
| `monitor_max_retry_delay` | int | `60000` | 5000–300000 | Yes (Explorer) | Ceiling for the backoff delay, in milliseconds. |
| `monitor_auto_restart` | bool | `false` | | Yes (Explorer) | Relaunch the shell if it does not come back. |
| `monitor_restart_delay` | int | `2000` | 500–10000 | Yes (Explorer) | Milliseconds before an auto-restart. |
| `explorer_notify_recovery` | bool | `true` | | Yes (Explorer) | Show a toast when the widget recovers. |

## `[TaskbarAutoHide]`

Keeps the widget in sync with an auto-hiding taskbar. Editable on the **Taskbar** tab.
See [Stability & Mitigations](../reference/stability.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `autohide_sync_enabled` | bool | `false` | | Yes (Taskbar) | Sync widget visibility with the auto-hiding taskbar. |
| `autohide_poll_interval` | int | `150` | 50–2000 | Yes (Taskbar) | Milliseconds between taskbar-state polls. |
| `autohide_hide_delay` | int | `200` | 0–5000 | Yes (Taskbar) | Milliseconds before hiding the widget. |
| `autohide_show_delay` | int | `0` | 0–5000 | Yes (Taskbar) | Milliseconds before showing the widget. |
| `autohide_use_fade` | bool | `true` | | Yes (Taskbar) | Fade the widget in/out with the taskbar. |
| `autohide_fade_duration` | int | `80` | 10–1000 | Yes (Taskbar) | Fade duration, in milliseconds. |
| `autohide_sync_desktop_list` | bool | `true` | | Yes (Taskbar) | Also hide the desktop list with the taskbar. |
| `autohide_sync_window_list` | bool | `false` | | Yes (Taskbar) | Also hide the window list with the taskbar. |
| `autohide_hidden_threshold` | int | `4` | 1–20 | Yes (Taskbar) | Pixel threshold for detecting a hidden taskbar. |
| `autohide_recheck_count` | int | `10` | 1–100 | Yes (Taskbar) | Recheck iterations used to debounce oscillation. |
| `autohide_skip_if_dialog` | bool | `true` | | Yes (Taskbar) | Don't hide while a dialog is open. |

## `[Notifications]`

Two groups: toast notifications (**Notifications** tab) and the on-screen display, whose keys are
prefixed `osd_` (**OSD** tab). All live in the `[Notifications]` INI section.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `notifications_enabled` | bool | `true` | | Yes (Notifications) | Master toggle for toast notifications. |
| `notify_window_moved` | bool | `false` | | Yes (Notifications) | Toast when a window is moved to another desktop. |
| `notify_desktop_created` | bool | `false` | | Yes (Notifications) | Toast when a desktop is created. |
| `notify_desktop_deleted` | bool | `false` | | Yes (Notifications) | Toast when a desktop is deleted. |
| `notify_window_pinned` | bool | `false` | | Yes (Notifications) | Toast when a window is pinned. |
| `notify_window_unpinned` | bool | `false` | | Yes (Notifications) | Toast when a window is unpinned. |
| `notify_explorer_recovery` | bool | `false` | | Yes (Notifications) | Toast when the widget recovers from an Explorer restart. |
| `notify_explorer_crash` | bool | `false` | | Yes (Notifications) | Toast when an Explorer crash is detected. |
| `osd_enabled` | bool | `false` | | Yes (OSD) | Show a large on-screen display on desktop switch. |
| `osd_show_name` | bool | `true` | | Yes (OSD) | Include the desktop name in the OSD. |
| `osd_show_number` | bool | `true` | | Yes (OSD) | Include the desktop number in the OSD. |
| `osd_duration` | int | `1500` | 500–5000 | Yes (OSD) | How long the OSD stays visible, in milliseconds. |
| `osd_position` | enum | `top-center` | nine-anchor grid plus `widget` | Yes (OSD) | Where the OSD appears. |
| `osd_font_size` | int | `14` | 8–48 | Yes (OSD) | OSD font size. |
| `osd_opacity` | int | `220` | 0–255 | Yes (OSD) | OSD opacity. |
| `osd_format` | string | `{number}: {name}` | template | Yes (OSD) | OSD text template; `{number}` and `{name}` are substituted. |
| `osd_width` | int | `300` | 100–800 | Yes (OSD) | OSD width, in pixels. |

## `[Rules]`

The window rules engine. The enable toggle and the rule editor are on the **Behavior** tab
(Rules subsection). Rule entries themselves (`rule_N`) are documented in
[Rules Engine & Hooks](../guides/rules-engine.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `rules_enabled` | bool | `false` | | Yes (Behavior) | Enable the polling rules engine. |
| `rules_poll_interval` | int | `2000` | 500–30000 | Yes (Behavior) | Milliseconds between rule evaluations. |
| `rule_1`, `rule_2`, … | string | *(none)* | `pattern\|target_desktop` | Yes (Behavior) | One rule per key. Edited through the Rules subsection (read/written directly as `rule_N` by `ConfigDialog.au3`); see the [Rules Engine](../guides/rules-engine.md) guide for syntax. |

## `[Session]`

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `session_restore_enabled` | bool | `false` | | INI-only | Restore windows to their desktops on launch. Advanced feature; no dialog control — enable it by editing the INI. See [Persistence & Profiles](../guides/persistence.md). |

## `[Hooks]`

External commands run on app events. No dialog controls — configure by editing the INI.
Individual `[Hooks]` event entries and their syntax are documented in
[Rules Engine & Hooks](../guides/rules-engine.md).

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `hooks_enabled` | bool | `false` | | INI-only | Enable running hook commands on events. |
| `hooks_timeout` | int | `10000` | 1000–300000 | INI-only | Milliseconds before a running hook is killed. |

## `[Profiles]`

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `profiles_enabled` | bool | `false` | | INI-only | Enable named configuration profiles. Advanced feature; no dialog control. See [Persistence & Profiles](../guides/persistence.md). |

## `[Slideshow]`

Automatic, configurable rotation through desktops — a dashboard/kiosk feature. Editable on the
**Behavior** tab, **Slideshow** sub-tab. Supersedes the former `[Carousel]` (see the migration
note below). Defaults and clamps are read in `_Cfg_Load` (`includes/Config.au3`); the participating
desktops, per-desktop timing, loops, and break conditions are resolved by the pure engine in
`includes/Slideshow.au3`.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `slideshow_enabled` | bool | `false` | | Yes (Behavior) | Master enable. Exposes the slideshow's tray item, context-menu entry, and hotkey. Does **not** auto-start on launch — that is `slideshow_autostart`. |
| `slideshow_interval` | int | `20000` | 1000–3600000 | Yes (Behavior) | Default milliseconds a desktop is shown before advancing. Per-desktop overrides come from `slideshow_desktop_intervals`. |
| `slideshow_show_in_menu` | bool | `true` | | Yes (Behavior) | Show the Start/Stop Slideshow entry in the tray and context menus. |
| `notify_slideshow_toggle` | bool | `true` | | Yes (Behavior) | Toast when the slideshow starts/stops. |
| `slideshow_selection_mode` | enum | `all` | `all`, `even`, `odd`, `name_contains`, `custom` | Yes (Behavior) | Which desktops participate: `all` = every desktop `1..N`; `even`/`odd` = even-/odd-numbered desktops; `name_contains` = desktops whose label contains `slideshow_name_filter` (case-insensitive substring); `custom` = the explicit `slideshow_sequence` list. |
| `slideshow_direction` | enum | `forward` | `forward`, `backward` | Yes (Behavior) | Composes with every selection mode: ascending vs. descending desktop order for `all`/`even`/`odd`/`name_contains`, and original vs. reversed list order for `custom`. |
| `slideshow_name_filter` | string | *(empty)* | ≤128 chars | Yes (Behavior) | Case-insensitive substring matched against desktop labels; used only by `name_contains`. Labels are snapshotted when the slideshow starts, so renaming a desktop mid-run does not re-filter it (restart to re-filter). An empty filter or zero matches makes the selection invalid — the slideshow refuses to start with a toast. |
| `slideshow_sequence` | string | *(empty)* | ≤256 chars | Yes (Behavior) | CSV of 1-based desktop numbers used only by `custom`, e.g. `1,3,2,5`; repeats are allowed. Non-numeric or out-of-range tokens are skipped; an empty result refuses to start. |
| `slideshow_desktop_intervals` | string | *(empty)* | ≤512 chars | Yes (Behavior) | Per-desktop dwell overrides as a `desktop:ms` CSV, e.g. `1:5000,3:8000`. Applies in **every** selection mode. Any step landing on a listed desktop uses its override; unlisted / zero / invalid desktops fall back to `slideshow_interval`. Override milliseconds are clamped to 500–3600000. |
| `slideshow_loop_mode` | enum | `infinite` | `infinite`, `count`, `duration` | Yes (Behavior) | When the slideshow stops on its own: `infinite` never stops; `count` stops after `slideshow_loop_count` full passes; `duration` stops after `slideshow_loop_duration` seconds. |
| `slideshow_loop_count` | int | `3` | 1–1000 | Yes (Behavior) | Full passes through the resolved selection before stopping (`count` mode). |
| `slideshow_loop_duration` | int | `300` | 5–86400 | Yes (Behavior) | Seconds to run before stopping (`duration` mode). |
| `slideshow_autostart` | bool | `false` | | Yes (Behavior) | Start the slideshow automatically at launch (after `slideshow_autostart_delay`). Requires `slideshow_enabled`. |
| `slideshow_autostart_delay` | int | `5000` | 0–3600000 | Yes (Behavior) | Milliseconds to wait after launch before auto-starting. |
| `slideshow_break_on_manual_switch` | bool | `true` | | Yes (Behavior) | Stop the slideshow if a desktop change happens that the slideshow did not command. |
| `slideshow_break_on_widget_click` | bool | `true` | | Yes (Behavior) | Stop the slideshow on any click on the widget. |
| `slideshow_break_on_hotkey` | bool | `true` | | Yes (Behavior) | Stop the slideshow on any app navigation/action hotkey. |
| `slideshow_break_on_any_input` | bool | `false` | | Yes (Behavior) | Stop the slideshow on any keyboard/mouse activity (via `GetLastInputInfo`). The slideshow's own switches generate no input, so they never trip this. |

Regardless of the break settings above, the toggle hotkey, tray item, context-menu entry, and
`--toggle-slideshow` always stop a running slideshow — a manual stop is always available.

**Migration from `[Carousel]`.** The old `[Carousel]` section is read once as a fallback and then
removed. On load, each new key falls back to its legacy counterpart for a default
(`slideshow_enabled` ← `carousel_enabled`, `slideshow_interval` ← `carousel_interval` re-clamped
from the old 3000–300000 range to 1000–3600000, `slideshow_show_in_menu` ← `carousel_show_in_menu`,
`notify_slideshow_toggle` ← `notify_carousel_toggle`), and `hotkey_toggle_slideshow` falls back to
`hotkey_toggle_carousel`. The first time the config is saved, the legacy `[Carousel]` section and
the `hotkey_toggle_carousel` key are deleted. A stored `tray_middle_click = toggle_carousel` is
normalized to `toggle_slideshow` on load.

## `[Tray]`

Tray-icon click actions and tooltip/menu contents (used when `tray_icon_mode` is on). Editable on
the **Tray** tab.

| Key | Type | Default | Range / Values | Settings UI | Notes |
|---|---|---|---|---|---|
| `tray_left_click` | enum | `menu` | `menu`, `toggle_list`, `next_desktop`, `nothing` | Yes (Tray) | Left-click action. |
| `tray_double_click` | enum | `settings` | `settings`, `toggle_list`, `menu`, `nothing` | Yes (Tray) | Double-click action. |
| `tray_middle_click` | enum | `toggle_list` | `toggle_list`, `add_desktop`, `toggle_slideshow`, `nothing` | Yes (Tray) | Middle-click action. A legacy `toggle_carousel` value is accepted and normalized to `toggle_slideshow` on load. |
| `tray_tooltip_show_label` | bool | `true` | | Yes (Tray) | Show the desktop label in the tray tooltip. |
| `tray_tooltip_show_count` | bool | `false` | | Yes (Tray) | Show the desktop count in the tray tooltip. |
| `tray_menu_show_list` | bool | `true` | | Yes (Tray) | Include the desktop list in the tray menu. |
| `tray_menu_show_edit` | bool | `true` | | Yes (Tray) | Include the rename/edit entry. |
| `tray_menu_show_add` | bool | `true` | | Yes (Tray) | Include the add-desktop entry. |
| `tray_menu_show_delete` | bool | `true` | | Yes (Tray) | Include the delete-desktop entry. |
| `tray_menu_show_desktop_submenu` | bool | `false` | | Yes (Tray) | Include a per-desktop submenu. |
| `tray_menu_show_move_window` | bool | `false` | | Yes (Tray) | Include the move-window entry. |
| `tray_notify_desktop_switch` | bool | `false` | | Yes (Tray) | Balloon on desktop switch. |
| `tray_balloon_duration` | int | `2000` | 500–10000 | Yes (Tray) | Balloon duration, in milliseconds. |
| `tray_close_to_tray` | bool | `false` | | Yes (Tray) | Closing the widget minimizes to tray instead of quitting. |

## Runtime-managed and hidden behavior

A few keys above are marked `INI-only` because they are written by the app at runtime rather than
edited through a control:

- `desktop_list_pinned`, `window_list_pinned` — set by the panels' pin toggles.
- `window_list_custom_x` / `window_list_custom_y` — set when you drag the window list panel.

The remaining `INI-only` keys (`name_sync_interval`, `dll_check_interval`, `update_poll_interval`,
`session_restore_enabled`, `hooks_enabled`, `hooks_timeout`, `profiles_enabled`) are genuine
edit-the-file settings with no dialog control.

### Hidden shortcuts

Some interactions are always available and are not bound through `[Hotkeys]`:

| Interaction | Action |
|---|---|
| Triple-click a desktop-list row | Open the rename dialog |
| Double-click the widget number | Quick-access mode — type a desktop number (when `quick_access_enabled`) |
| Escape | Cancel a drag, close dialogs, cancel quick-access |
| Mouse wheel on the widget | Cycle desktops (when `scroll_enabled`) |
| Mouse wheel on the desktop list | Scroll the list or switch desktops (per `list_scroll_action`) |
| Mouse wheel on the Settings dialog | Scroll the current tab's content |

### Related state files

`desk_switcheroo.ini` is not the only file the app maintains. `desk_switcheroo_state.ini` stores
transient state such as the list scroll offset (restored on next launch), `desktop_labels.ini`
holds desktop labels, and profiles are stored as separate INI files. Crash reports are written to
`crash_YYYYMMDD_HHMMSS.log` (falling back to `%TEMP%` if the script directory is not writable).
See [Persistence & Profiles](../guides/persistence.md) for the full picture.
